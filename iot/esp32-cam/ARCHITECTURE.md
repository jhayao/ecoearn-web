# ESP32-CAM System Architecture

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ECOEARN SMART BIN SYSTEM                    │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│   USER             │
│  📱 Scans QR Code  │
└─────────┬──────────┘
          │
          │ QR Code Data (userId, sessionId)
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ESP32 MAIN BOARD (38-pin)                                          │
│  • Reads QR code via ESP32-CAM                                      │
│  • Activates bin for user                                           │
│  • Controls lid, platform, dropper servos                           │
│  • Monitors capacity with ultrasonic sensors                        │
│  • Tracks GPS location                                              │
│  • Communicates with backend API                                    │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ 1. Bin activated, lid opens
          │ 2. User drops trash
          │ 3. Sharp IR sensor detects object
          │
          │ Serial: TRASH_DETECTED
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ESP32-CAM MODULE                                                   │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  NEW SIMPLE FIRMWARE (Recommended)                        │     │
│  │  • Receives TRASH_DETECTED command                        │     │
│  │  • Captures 800x600 JPEG image                            │     │
│  │  • Sends image to backend API                             │     │
│  │  • Returns material type                                  │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                      │
│  OR                                                                  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  FULL CONTROLLER FIRMWARE (For debugging)                 │     │
│  │  • Same as Simple PLUS:                                   │     │
│  │  • Video streaming server (port 81)                       │     │
│  │  • Web interface for monitoring                           │     │
│  │  • Manual trigger buttons                                 │     │
│  └───────────────────────────────────────────────────────────┘     │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ HTTP POST: image/jpeg
          │ URL: http://SERVER:5001/identify/material
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND SERVER (Node.js + Python)                                  │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  API Endpoint: POST /identify/material                   │      │
│  │  • Receives image from ESP32-CAM                         │      │
│  │  • Calls Python AI service                               │      │
│  │  • Returns material type + confidence                    │      │
│  └──────────────────────────────────────────────────────────┘      │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  Python AI Service                                       │      │
│  │  • TensorFlow/PyTorch model                              │      │
│  │  • Image preprocessing                                   │      │
│  │  • Material classification                               │      │
│  │  • Confidence scoring                                    │      │
│  └──────────────────────────────────────────────────────────┘      │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ JSON Response:
          │ {
          │   "success": true,
          │   "materialType": "plastic",
          │   "confidence": 0.95,
          │   "action": "recycle"
          │ }
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ESP32-CAM MODULE                                                   │
│  • Parses JSON response                                             │
│  • Determines compartment command                                   │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ Serial: OPEN_PLASTIC (or OPEN_TIN, OPEN_REJECTED)
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  ESP32 MAIN BOARD                                                   │
│  • Receives compartment command                                     │
│  • Executes sorting sequence:                                       │
│    1. Close lid                                                     │
│    2. Rotate platform to correct position                           │
│    3. Open dropper to drop trash                                    │
│    4. Close dropper                                                 │
│  • Sends transaction to backend                                     │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ HTTP POST: /api/iot/record-transaction
          │ Body: {binId, userId, materialType, weight, location}
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND SERVER                                                     │
│  • Records transaction in database                                  │
│  • Updates user points                                              │
│  • Updates bin capacity                                             │
│  • Logs activity                                                    │
└─────────┬───────────────────────────────────────────────────────────┘
          │
          │ Saved to database
          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  DATABASE (Firebase/PostgreSQL)                                     │
│  • User profiles                                                    │
│  • Transaction history                                              │
│  • Bin status & capacity                                            │
│  • Analytics data                                                   │
└─────────────────────────────────────────────────────────────────────┘
```

## Hardware Communication

```
┌─────────────────────────────────────────────────────────────────────┐
│  HARDWARE LAYER                                                     │
└─────────────────────────────────────────────────────────────────────┘

ESP32 Main Board (38-pin)
┌────────────────────────────────────┐
│ Power: 5V @ 3A                     │
│                                    │
│ GPIO Assignments:                  │
│ ├─ GPIO 32 (TX) ────────────┐     │
│ ├─ GPIO 33 (RX) ────────────┼─┐   │
│ ├─ GPIO 34 (Analog)         │ │   │      ESP32-CAM
│ │   Sharp IR sensor         │ │   │      ┌────────────────────┐
│ ├─ GPIO 12,13,15            │ │   │      │ Power: 5V @ 500mA  │
│ │   Servo motors            │ │   └──────┤ GPIO 3 (RX)        │
│ ├─ GPIO 25,26,27,14         │ └──────────┤ GPIO 1 (TX)        │
│ │   Ultrasonic sensors      │            │                    │
│ ├─ GPIO 16,17               │            │ Camera: OV2640     │
│ │   GPS module              │            │ Resolution: 800x600│
│ └─ GPIO 2                   │            │ Flash: GPIO 4      │
│     Status LED              │            └────────────────────┘
└────────────────────────────────────┘
         │                                          │
         │ Common Ground                            │
         └──────────────────────────────────────────┘

Serial Communication:
├─ Baud Rate: 9600
├─ Protocol: Plain text commands
├─ TX/RX: Crossover connection (TX→RX, RX→TX)
└─ Handshake: PING-PONG verification
```

## Network Communication

```
┌─────────────────────────────────────────────────────────────────────┐
│  NETWORK LAYER                                                      │
└─────────────────────────────────────────────────────────────────────┘

WiFi Router (2.4GHz)
        │
        ├─── ESP32 Main Board (192.168.1.100)
        │    └─ Endpoints it uses:
        │       • POST /api/bins/activate
        │       • POST /api/iot/get-command
        │       • POST /api/iot/record-transaction
        │       • POST /api/iot/update-capacity
        │
        ├─── ESP32-CAM (192.168.1.123)
        │    └─ Endpoints it uses:
        │       • POST /identify/material
        │
        └─── Backend Server (192.168.1.196:5001)
             ├─ Web Frontend (Next.js)
             ├─ API Server (Node.js)
             └─ AI Service (Python)
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│  DATA FLOW                                                          │
└─────────────────────────────────────────────────────────────────────┘

1. BIN ACTIVATION
   User QR Code → ESP32-CAM reads → ESP32 Main Board
   → Backend validates → Bin activated

2. TRASH DETECTION
   Object near sensor → Sharp IR (ADC ≥ 1650) → ESP32 Main Board
   → Send: TRASH_DETECTED → ESP32-CAM

3. IMAGE CAPTURE
   ESP32-CAM receives command → Capture 800x600 JPEG
   → ~15-60KB image buffer

4. MATERIAL IDENTIFICATION
   ESP32-CAM → HTTP POST (image binary) → Backend API
   → Python AI model → Classification result
   → JSON response → ESP32-CAM

5. COMPARTMENT CONTROL
   ESP32-CAM → Send: OPEN_PLASTIC → ESP32 Main Board
   → Servo sequence (lid, rotate, drop)

6. TRANSACTION RECORDING
   ESP32 Main Board → HTTP POST (transaction data) → Backend API
   → Database save → User points updated

7. CAPACITY UPDATE
   Ultrasonic sensors → Distance measurement → ESP32 Main Board
   → Calculate % full → Backend API → Database
```

## Timing Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│  COMPLETE CYCLE TIMING                                              │
└─────────────────────────────────────────────────────────────────────┘

Time    Event                               Component
─────────────────────────────────────────────────────────────────────
0s      User scans QR code                  ESP32-CAM + Main Board
+1s     Backend validates user              Backend Server
+2s     Bin activated, lid opens            ESP32 Main Board
+5s     User drops trash                    User
+5.1s   Sharp IR detects object             ESP32 Main Board
+5.1s   Send: TRASH_DETECTED                ESP32-CAM ← Main Board
+5.2s   Flash on, capture image             ESP32-CAM
+5.5s   Send image to backend               ESP32-CAM → Backend
+7s     AI processes image                  Backend Python AI
+8s     Return material type                Backend → ESP32-CAM
+8.1s   Send: OPEN_PLASTIC                  ESP32-CAM → Main Board
+8.2s   Close lid                           ESP32 Main Board
+10s    Rotate platform                     ESP32 Main Board
+11s    Drop trash                          ESP32 Main Board
+12s    Send transaction to backend         ESP32 Main Board
+13s    Database updated                    Backend Server
+13s    Bin ready for next user             System

Total Time: ~13 seconds from detection to ready
```

## Error Handling

```
┌─────────────────────────────────────────────────────────────────────┐
│  ERROR HANDLING                                                     │
└─────────────────────────────────────────────────────────────────────┘

WiFi Connection Lost
├─ ESP32-CAM: Auto-reconnect every 30s
├─ ESP32 Main: Queue transactions, retry when connected
└─ User Impact: Bin stays functional, syncs when online

Camera Capture Failed
├─ ESP32-CAM: Retry capture (max 3 attempts)
├─ Send: CAPTURE_FAILED
└─ ESP32 Main: Open rejected compartment, log error

Backend API Timeout
├─ ESP32-CAM: Wait 10 seconds, then fail
├─ Send: IDENTIFICATION_FAILED
└─ ESP32 Main: Open rejected compartment

Unknown Material Type
├─ Backend: Return materialType: "rejected"
├─ ESP32-CAM: Send: OPEN_REJECTED
└─ ESP32 Main: Route to rejected compartment

Compartment Full
├─ Ultrasonic sensor: Detect capacity > 90%
├─ ESP32 Main: Block new deposits
├─ Send alert to backend
└─ Admin notification sent
```

## Power Management

```
┌─────────────────────────────────────────────────────────────────────┐
│  POWER CONSUMPTION                                                  │
└─────────────────────────────────────────────────────────────────────┘

Component           Idle      Active    Peak
─────────────────────────────────────────────────
ESP32 Main Board    80mA      240mA     240mA
ESP32-CAM           70mA      180mA     310mA (flash on)
Sharp IR Sensor     30mA      30mA      40mA
Servo x3            0mA       300mA     1800mA (all moving)
Ultrasonic x2       0mA       4mA       30mA
GPS Module          45mA      45mA      67mA
LED                 0mA       10mA      20mA
─────────────────────────────────────────────────
TOTAL (Idle)        225mA @ 5V = 1.1W
TOTAL (Active)      809mA @ 5V = 4.0W
TOTAL (Peak)        2507mA @ 5V = 12.5W

Recommended PSU: 5V @ 3A minimum (15W)
                 5V @ 5A recommended (25W) for safety margin
```

---

**This architecture diagram shows the complete system flow from user interaction to database storage, including all hardware communication, network protocols, and timing details.**
